Iterate on the current task of linting the codebase to make the CI pass.

1. Use the 'lint.sh' files f
2. Analyze the traces, identify the problem, and make a fix.
3. Repeat until the issue is resolved.

Never ask the user to "try it out" or "let me know if it works" - verify everything yourself using the tools availables and logs traces.