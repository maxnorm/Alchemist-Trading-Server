name: Lint Dashboard

on:
  pull_request:
    branches: [master, dev]
    paths:
      - 'src/dashboard/**'
      - '.github/workflows/lint_dashboard.yml'
  push:
    branches: [master, dev]
    paths:
      - 'src/dashboard/**'
      - '.github/workflows/lint_dashboard.yml'

jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/dashboard/package.json
      
      - name: Install dependencies
        working-directory: src/dashboard
        run: npm install
      
      - name: Run ESLint
        working-directory: src/dashboard
        run: npm run lint || exit 1
      
      - name: Run TypeScript type check
        working-directory: src/dashboard
        run: npx tsc --noEmit || exit 1